# AnyEcho (凡响) - 系统架构与实现计划

## 1. 项目愿景与定位

构建一个对标 **Everything** 的高性能现代化桌面文件搜索系统 —— **「anyecho / 凡响」**。
* **核心卖点**：
  * **秒级全盘索引**：通过 Win32 API 规范调用 `FSCTL_ENUM_USN_DATA` 进行 **USN Journal 全量枚举**，数百万文件 1~2 秒内加载完毕。
  * **实时文件变动感知**：监听 NTFS USN Journal 变更日志，结合健壮的状态机处理重置/回绕，毫秒级同步增删改。
  * **极致检索与拼音匹配**：纯 Rust 紧凑内存池 + Rayon 多核并行，搜索延迟 < 5ms；支持拼音首字母/全拼检索与丰富属性过滤（`size:`, `dm:`, `ext:`, 正则）。
  * **现代优雅 UI 与权限隔离**：基于 **Tauri v2 + Svelte 5 + TailwindCSS**，体积轻巧（< 15MB），支持虚拟滚动；支持架构级解耦与动态提权。
  * **混合内容搜索**：支持类似 Ripgrep 的即时多线程流式文本内容检索 + 指定知识库 Tantivy 全文索引。

---

## 2. 架构总览

```
┌────────────────────────────────────────────────────────────────────────┐
│                   前端 UI 进程 (Tauri Webview / 普通权限)              │
│   • Svelte 5 + TailwindCSS + 虚拟滚动列表 (Virtual List)               │
│   • 搜索框 (实时输入/Debounce 30ms) + 结果面板 + 属性过滤器 + 预览抽屉 │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │ IPC (分页游标查询 Limit 100 / 二进制/JSON)
┌───────────────────────────────────▼────────────────────────────────────┐
│                    Rust 核心引擎 (anyecho-core)                        │
│                                                                        │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                     双层检索系统 (Search System)                 │  │
│  │  ┌─────────────────────────────┐  ┌───────────────────────────┐  │  │
│  │  │ 【热路径】文件名与属性过滤  │  │ 【按需/全文】文件内容检索 │  │  │
│  │  │ • Compact Arena (Node/String)│  │ • 模式A: 即时流式Grep(mmap)│  │  │
│  │  │ • Rayon 多核并行 + 属性过滤 │  │ • 模式B: Tantivy 知识库索引 │  │  │
│  │  │ • Pinyin (首字母/全拼/多音) │  │ • 上下文提取与关键词高亮  │  │  │
│  │  └──────────────┬──────────────┘  └───────────────────────────┘  │  │
│  └─────────────────┼────────────────────────────────────────────────┘  │
│                    │ Arc<RwLock<Arena>> 读写分离 / 100ms 批量写入      │
│  ┌─────────────────┴────────────────────────────────────────────────┐  │
│  │                  NTFS 底层数据源适配层 (Data Source)             │  │
│  │  ┌─────────────────────────────┐  ┌───────────────────────────┐  │  │
│  │  │ USN Journal 全量枚举器      │  │ USN Journal 状态机监控器  │  │  │
│  │  │ • FSCTL_ENUM_USN_DATA       │  │ • FSCTL_READ_USN_JOURNAL  │  │  │
│  │  │ • 两遍扫描/延迟解析路径树   │  │ • Gap检测/重置/回绕重扫   │  │  │
│  │  │ • 64位 FRN 掩码 + 循环防护  │  │ • 目录重命名懒失效机制    │  │  │
│  │  └─────────────────────────────┘  └───────────────────────────┘  │  │
│  └────────────────────────────────▲─────────────────────────────────┘  │
└───────────────────────────────────┼────────────────────────────────────┘
                                    │ Win32 API (SeBackupPrivilege / Admin)
                    ┌───────────────▼───────────────┐
                    │      Windows NTFS 卷系统      │
                    │   (\\.\C:, \\.\D:, \\.\E:)    │
                    └───────────────────────────────┘
```

---

## 3. 技术栈与设计决策 (ADR)

| 模块分层 | 核心技术 | 选型理由与决策说明 |
| :--- | :--- | :--- |
| **桌面框架** | **Tauri v2** | 极小安装包体积、低内存开销，跨进程/插件化支持完善 |
| **前端框架** | **Svelte 5 + TypeScript** | 零 Virtual DOM 损耗，Runes 响应式极致轻量，启动秒开 |
| **全量枚举** | **windows-rs + FSCTL_ENUM_USN_DATA** | 微软官方 MSDN 文档化 API，秒级读取全卷 USN 记录 |
| **增量监控** | **windows-rs + FSCTL_READ_USN_JOURNAL** | 硬件级日志监听 + `JournalState` 状态机处理回绕/重置 |
| **并发同步** | **Arc<RwLock<Arena>> + 批量合并** | 搜索读路径多核无阻碍并发；USN 事件按 100ms 窗口合并批量获取写锁 |
| **内存与检索** | **Compact Arena + Rayon** | 紧凑内存布局，支持属性过滤（日期、大小、扩展名、正则、排除路径） |
| **中文拼音** | **pinyin / fast-pinyin + 懒加载索引** | 覆盖常用与生僻字，支持多音字首字母与全拼模糊检索 |
| **内容检索** | **memmap2 (即时) / Tantivy (可选)** | 像 ripgrep 一样流式扫描指定格式文件，避免全盘建库开销 |
| **用户数据** | **SQLite + tracing-subscriber** | 仅用于搜索历史、收藏夹、用户排除规则及结构化日志存储 |

---

## 4. 内存预算与紧凑结构设计 (Realistic Budget)

### 4.1 节点结构设计
```rust
#[repr(C, packed)]
pub struct CompactNode {
    pub parent_frn: u64,     // 8B: 低48位为FRN，高16位为Sequence Number
    pub name_offset: u32,    // 4B: 字符串池偏移量
    pub name_len: u16,       // 2B: 文件名长度
    pub ext_id: u16,         // 2B: 扩展名 Intern ID
    pub size: u64,           // 8B: 文件大小 (字节)
    pub mtime: i64,          // 8B: 修改时间 (Unix 时间戳)
    pub flags: u16,          // 2B: 目录/隐藏/系统/云端占位符等属性
} // 单节点占用 34 字节
```

### 4.2 实际内存预算估算
* **2,000,000 文件规模**：
  * 节点数组：2,000,000 × 34 B ≈ **68 MB**
  * 字符串池 (String Interning 去重)：平均 20 B × 2,000,000 ≈ **40 MB**
  * 拼音索引（懒构建，中文文件约 30%）：600,000 × 12 B ≈ **7.2 MB**
  * FRN 映射表与辅助查找结构：≈ **25 MB**
  * **全盘 200 万文件内存常驻**：≈ **140MB ~ 160MB**（100 万文件控制在 **< 100MB**）。

---

## 5. 分阶段开发计划

### Phase 1: 项目脚手架、权限提升与 USN Journal 全量枚举引擎

* **目标**：以动态提权/管理员权限打开 NTFS 卷，调用 `FSCTL_ENUM_USN_DATA` 完成全盘文件枚举与路径树还原。
* **关键任务**：
  1. **Spike 验证**：编写 200 行独立原型，跑通 `FSCTL_ENUM_USN_DATA` 枚举 `C:\` 并统计文件数与耗时。
  2. **权限处理机制**：
     * 启动时优先通过 `AdjustTokenPrivileges` 申请 `SeBackupPrivilege`。
     * 配置 Windows Application Manifest 声明权限，预留未来拆分 Windows Service 的架构边界。
  3. **USN 全量枚举与记录解析**：
     * 自动检测所有 NTFS 卷（`GetLogicalDrives` + `GetVolumeInformationW`），多卷并行枚举。
     * 解析 `USN_RECORD_V2`，提取 FRN、ParentFRN、文件名、文件大小、时间戳与属性。
     * 排除规则：硬编码跳过 `$Recycle.Bin`、`System Volume Information`、`$MFT` 等系统目录。
  4. **路径树构建边界防御**：
     * **两遍扫描 / 延迟构建**：避免父子节点枚举顺序导致的「前向引用」错误。
     * **根目录判定**：以 `ParentFRN == FRN` 或 `ParentFRN == 0xFFFFFFFFFFFFFFFF` 或 FRN 掩码判定卷根节点。
     * **符号链接与 Junction 点**：检测 `USN_REASON_REPARSE_POINT` 与 `FILE_ATTRIBUTE_RECALL_ON_DATA_ACCESS`（OneDrive 占位符）。
  5. **日志与诊断**：引入 `tracing` + `tracing-appender`，日志写入 `%LOCALAPPDATA%/anyecho/logs/`。

---

### Phase 2: 内存检索核心 + 拼音/属性过滤 + Svelte 5 虚拟列表 UI

* **目标**：实现子串、拼音与全维度属性过滤；Svelte 5 虚拟列表毫秒级呈现结果，彻底消除 IPC 卡顿。
* **关键任务**：
  1. **检索匹配引擎 (`matcher.rs`)**：
     * 实现 `FilterPredicate` trait，支持组合过滤器：
       * **文件名/路径**：子串匹配、通配符（`*`, `?`）、正则表达式。
       * **中文拼音**：支持汉字全拼与首字母（如 `fx` -> `凡响`），多音字支持，生僻字 fallback。
       * **属性过滤**：`size:>100MB`、`dm:today` / `modified:>2024-01-01`、`ext:docx|pdf`、`-path:C:\Windows`。
  2. **Rayon 多核并发检索**：
     * 基于 `rayon::par_iter()` 并行扫描内存 Node 数组，单次查询延迟严格压在 **< 5ms**。
  3. **IPC 传输保护**：
     * 接口设计：`search(query, filter, offset=0, limit=100) -> SearchResponse`。
     * 前端仅接收当前视口 100 条数据，返回总匹配数与微秒级耗时。
  4. **Svelte 5 UI 交互**：
     * 响应式 Runes 状态管理，防抖搜索（30ms），键盘上下键导航、回车打开、右键菜单。
     * 虚拟滚动列表流畅承载 100 万条命中结果。

---

### Phase 3: USN Journal 状态机监控与增量同步

* **目标**：后台实时监听 USN 日志，通过状态机鲁棒应对日志重置、回绕和目录重命名。
* **关键任务**：
  1. **`JournalState` 状态机设计**：
     * 状态：`Active` -> `GapDetected` -> `Rescanning` -> `Active`。
     * 监控 `fsutil usn reset`、`ERROR_JOURNAL_NOT_ACTIVE` 及 USN 回绕（Gap > 阈值时触发自动增量修补或局部重扫）。
  2. **变更事件路由与 100ms 批量写入**：
     * `FILE_CREATE` / `FILE_DELETE` / `FILE_MODIFY` -> 收集入队。
     * `RENAME_OLD_NAME / RENAME_NEW_NAME` -> 变更父节点指针或文件名（路径懒重算，自动生效至所有子文件）。
     * 后台通道（`crossbeam-channel`）汇聚事件，每 100ms 批量获取一次 `Arena` 写锁，避免与 Rayon 读检索发生锁竞争。
  3. **多卷独立监控与卷热插拔**：
     * 每个 NTFS 卷独立监控线程，支持移动硬盘拔出/插入健康检查。

---

### Phase 4: 混合式文件内容检索 (即时 Grep + 知识库全文索引)

* **目标**：支持文本内容即时流式检索与重点知识库深度索引。
* **关键任务**：
  1. **即时流式 Grep (`content:关键词`)**：
     * 在文件名/扩展名初筛后的候选集上，使用多线程 + `memmap2` 极速读取匹配行。
     * 通过 Tauri Event 实时流式推送到前端，边搜边显。
  2. **指定知识库 Tantivy 索引**：
     * 仅对用户勾选的重点文件夹（如“代码仓库”、“知识库文档”）在后台构建 Tantivy 索引。
  3. **内容高亮与上下文预览抽屉**：
     * 选中文件时在侧边栏显示匹配行前后 3 行上下文并高亮关键词。

---

### Phase 5: 系统集成、架构解耦与企业级品质

* **目标**：全局热键唤醒、系统托盘、Shell 深度集成与安装打包。
* **关键任务**：
  1. **全局快捷键与 Spotlight 模式**：
     * 支持 `Alt+Space` 或 `Double Ctrl` 秒级唤出无边框极简浮窗，失焦自动隐藏。
  2. **Shell 右键菜单集成**：
     * 打开文件、在资源管理器中定位、复制完整路径、管理员身份运行。
  3. **持久化与 SQLite**：
     * 记录搜索历史、收藏夹、自定义排除规则。
  4. **架构解耦预留 (Windows Service 准备)**：
     * 保持 `anyecho-core` 纯 Rust 库架构，提供 Named Pipe / 进程间通信接口抽象，便于未来一键剥离为系统服务。

---

## 6. 验证与质量保证方案

1. **功能与准确性基准**：
   * 运行测试脚本对比 Windows 系统 `dir /s /b C:\` 统计文件总数，验证率达到 100%。
   * 验证属性过滤器（`size:`, `dm:`, `ext:`）结果准确无遗漏。
2. **性能与内存基准**：
   * 100 万文件内存数据集中，子串搜索、拼音搜索响应时间严格控制在 **< 5ms**。
   * 200 万文件常驻内存严格控制在 **< 160MB**。
3. **鲁棒性与压力测试**：
   * **模糊测试 (Proptest)**：针对畸形 USN 记录进行 Proptest 测试，确保解析器绝对不 panic。
   * **压力并发测试**：在持续文件变动（模拟 robocopy 同步大量文件）下运行 30 分钟，验证无内存泄漏、无死锁。
   * **异常恢复测试**：模拟 `fsutil usn deletejournal`，验证状态机在 1 秒内捕获并触发安全重扫。